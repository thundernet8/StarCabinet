export default interface ITag {
    id: number;
    name: string;
    description: string;
    repos: number[];
    createdAt: string;
    createdTime: number;
    updatedAt: string;
    updatedTime: number;
}
